@echo off
REM Copy this file to local_settings.bat (already gitignored, never commit
REM your real credentials path) and fill in the real path below.
REM run_app.bat picks this up automatically if it exists.

set GOOGLE_APPLICATION_CREDENTIALS=C:\path\to\your\service-account-key.json
